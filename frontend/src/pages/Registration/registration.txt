* {
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 0;
  height: 100%;
  width: 100%;
  font-family: "Montserrat", sans-serif;
}

body .entry-container {
  min-height: 100vh;
  width: 100vw;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  align-items: center;
  background-color: #8dd3bb;
  background-image: url('/assets/backgroundill.png');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  padding: 80px 20px 20px 20px;
  overflow-y: auto;
  z-index: 1;
}

.logo img {
  height: 50px;
  display: block;
  margin-bottom: 30px;
}

.slogan {
  font-size: 14px;
  color: white;
  margin-bottom: 30px;
}

.card-container {
  display: flex;
  justify-content: center;
  gap: 60px;
  flex-wrap: wrap;
  margin-bottom: 30px;
}

.card {
  background: white;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
  padding: 30px;
  width: 200px;
  text-align: center;
  transition: transform 0.2s;
}

.card:hover {
  transform: scale(1.05);
}

.card-icon .bed-icon {
  height: 75px;
  margin-top: 10px;
}

.card-icon .taxi-icon {
  height: 70px;
  margin-top: 10px;
}

.card-icon .guide-icon {
  height: 60px;
  margin-top: 10px;
}

.card p {
  color: black;
  font-size: 14px;
  margin-bottom: 10px;
}

/* Updated button styles with specific class names */
.register-card-btn {
  background-color: #8dd3bb;
  margin-top: 10px;
  padding: 10px;
  width: 100%;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  color: white;
  font-size: 14px;
  font-weight: 500;
}

.register-card-btn:hover {
  background-color: #7ac8ac;
}

/* Specific button variations if needed */
/* Removed empty rulesets for .hotel-register-btn, .taxi-register-btn, and .guide-register-btn */
  /* Add specific styles for guide button if needed */


.card .guide-icon {
  height: 60px;
  margin: 7px;
}

.taxi-card p{
  margin-top:5px ;
}

.taxi-card .taxi-icon{
  transform: translateY(-2px);
}

.guide-card p{
  margin-top: 14px;
}

.guide-card .guide-icon{
  transform: translateY(6px);
}

/* Updated "Not now" button class name */
.registration-skip-btn {
  background: none;
  border: none;
  font-size: 14px;
  color: white;
  cursor: pointer;
}

.registration-skip-btn:hover {
  color: black;
}

.no-padding-wrapper {
  padding: 0 !important;
  margin: 0;
  max-width: 100vw;
}